# Telegram AI Bot with Context

Telegram-бот с поддержкой контекста и вызовом модели через HuggingFace Inference API.

## Установка

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
